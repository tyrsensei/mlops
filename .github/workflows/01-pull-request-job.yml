name: Pull request triggered job

on:
  pull_request:

jobs:
  train:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "Will add code checks here in next challenge !"